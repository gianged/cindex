/**
 * Minimal JavaScript file (<50 lines)
 * Used to test chunk size constraints
 */

export const VERSION = '1.0.0';

export function add(a, b) {
  return a + b;
}

export function multiply(a, b) {
  return a * b;
}
